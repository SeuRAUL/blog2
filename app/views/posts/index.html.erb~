<h1 id="list">Listing posts</h1>

<hr/>
<table>
  <tr>
    <td>Name</td>
    <td>Title</td>
    <th>Content</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @posts.each do |post| %>
  <tr>
    <td><%= post.title %></td>
    <td><%= post.content %></td>
    <td><%= post.name %></td>
    <td><%= link_to 'Show', post %></td>
    <td><%= link_to 'Edit', edit_post_path(post) %></td>
    <td><%= link_to 'Destroy', post, method: :delete, data: { confirm: 'Are you sure?' } %><br/></td>
  </tr>
<% end %>
</table>
<hr/>

<br />

<%= link_to 'New Post', new_post_path %>
